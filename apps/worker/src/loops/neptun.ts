import type { Loop } from './index';

/** Owned by the NEPTUN alerts unit. Placeholder: returns immediately until implemented. */
export const neptunLoop: Loop = {
  name: 'neptun',
  start: async () => {},
  stop: async () => {},
};
