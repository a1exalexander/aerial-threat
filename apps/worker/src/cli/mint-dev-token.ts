/** `cli mint-dev-token`. Owned by the operator auth unit. Returns the process exit code. */
export async function run(_argv: string[]): Promise<number> {
  console.error('mint-dev-token: not implemented');
  return 2;
}
