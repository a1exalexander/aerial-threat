/** `cli eval-live`. Owned by the Jev AI adapter unit. Returns the process exit code. */
export async function run(_argv: string[]): Promise<number> {
  console.error('eval-live: not implemented');
  return 2;
}
