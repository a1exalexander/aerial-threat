/** `cli replay`. Owned by the Telegram export import unit (replay). Returns the process exit code. */
export async function run(_argv: string[]): Promise<number> {
  console.error('replay: not implemented');
  return 2;
}
