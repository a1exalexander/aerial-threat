/** `cli import`. Owned by the Telegram export import unit. Returns the process exit code. */
export async function run(_argv: string[]): Promise<number> {
  console.error('import: not implemented');
  return 2;
}
